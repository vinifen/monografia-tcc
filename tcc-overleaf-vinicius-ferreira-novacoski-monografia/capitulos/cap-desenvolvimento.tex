%%%% CAPÍTULO — DESENVOLVIMENTO DO SISTEMA
%%
%% Corpo principal da monografia (TCC~2): o que foi feito no E-Museu.
%% Alinhar seções aos objetivos específicos e às evidências do repositório
%% (PRs, issues, wiki, deploy). Redigir em tempo verbal passado.

\chapter{Desenvolvimento do Sistema}\label{cap:desenvolvimento}

Este capítulo apresenta o desenvolvimento realizado na refatoração e na implementação de novas funcionalidades no E-Museu, desde o planejamento até a entrega operacional na \acrshort{utfpr}.


\section{Dimensão do esforço e dos recursos}\label{sec:dev-esforco}

O desenvolvimento descrito neste capítulo demandou investimento continuado de tempo e de recursos financeiros do autor, acompanhados de forma sistemática para estimar a magnitude do trabalho.

Quanto ao tempo, o desenvolvimento teve início em dezembro de 2025 e conclusão em maio de 2026. Nesse intervalo, dedicou-se cerca de 250~horas ao conjunto das atividades ligadas ao E-Museu e a este TCC (implementação, infraestrutura, documentação, deslocamentos à \acrshort{utfpr} e redação da monografia). O registro foi feito diariamente na ferramenta descrita na Seção~\ref{sec:materiais-notion}, com a descrição das tarefas realizadas e da duração de cada sessão.

Quanto aos custos, o autor despendeu aproximadamente R\$\,400,00 ao longo do percurso do projeto. Esse montante inclui, entre outros itens: assinatura do \textit{Cursor}; hospedagem da VPS na DigitalOcean para ensaios com \textit{Coolify}; deslocamentos ao campus da \acrshort{utfpr} para implantação e manutenção do servidor; aquisição de pen drive para o pacote de continuidade entregue à orientação; e licença do Overleaf na disciplina de TCC~1, quando o serviço não era disponibilizado gratuitamente pelos professores. Esses recursos complementaram a infraestrutura principal mantida na universidade e viabilizaram as entregas descritas nas seções seguintes.


\section{Levantamento de requisitos e priorização}\label{sec:dev-requisitos}

O levantamento de requisitos constituiu a primeira etapa do trabalho. Os requisitos foram identificados principalmente a partir das observações da orientadora Dra. Sediane Carmem Lunardi Hernandes, que apontou, no sistema E-Museu, a necessidade de aprimoramentos na estrutura do código, bem como a ausência de testes automatizados, evidenciando a demanda por refatoração e pelo desenvolvimento de novas funcionalidades.

Após discussões e alinhamentos, os requisitos receberam ajustes a partir das contribuições do coorientador Dr. Diego Marczal e dos professores Me. Dênis Lucas Silva e Dr. Emerson André Fedechen, especialmente durante a primeira banca, o que permitiu um refinamento mais preciso e alinhado aos objetivos centrais do trabalho.

Com os requisitos definidos, foi aplicada a técnica de priorização MoSCoW, com o objetivo de direcionar o desenvolvimento para as funcionalidades de maior impacto no sistema, otimizando o uso do tempo, esforço e recursos disponíveis. O resultado dessa priorização é apresentado na Figura~\ref{fig:moscow}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{figuras/moscow.png}
    \caption{Priorização \acrshort{moscow}.}
    \label{fig:moscow}
    \caption*{\textbf{Fonte:} Autoria própria.}
\end{figure}


\section{Organização do repositório e fluxo de desenvolvimento}\label{sec:dev-gitflow}

\subsection{Documentação do Gitflow e convenções}

Para garantir um fluxo de desenvolvimento consistente e devidamente documentado, foi realizada, previamente ao início da implementação, uma elaboração completa da documentação referente ao Gitflow, convenções de \textit{commits}, gestão de \textit{issues} e demais práticas relacionadas ao versionamento. Toda essa documentação está disponível no repositório oficial do projeto \cite{gitflowdoc}.

% TODO: exemplos concretos de PRs/issues no perfil full (branches @vinifen/…, commits com emoji).

\subsection{Criação do repositório e versionamento}

O repositório do E-Museu foi criado a partir de um \textit{fork}, que se trata de uma cópia independente de um projeto existente, permitindo sua evolução de forma separada da primeira versão desenvolvida por \cite{gonzaga2024}. A partir dele, estruturou-se uma configuração inicial de ambiente utilizando Docker Compose, servindo como base para o desenvolvimento posterior \cite{emuseurepo}.

% TODO: síntese do histórico de PRs/issues relevantes (sem depender de links externos no corpo).


\section{Configuração de ambientes e infraestrutura}\label{sec:dev-infra}

A maior parte do desenvolvimento ocorreu no ambiente local containerizado descrito no Capítulo~\ref{cap:materiais-e-metodos} (Docker Compose, script \texttt{run} e integração contínua no \textit{GitHub}). Em paralelo, utilizou-se uma VPS na DigitalOcean com \textit{Coolify} instalado para simular \textit{staging} e produção, validar os arquivos \texttt{docker-compose.staging.yml} e \texttt{docker-compose.production.yml} e ensaiar \textit{webhooks} antes da replicação institucional.

Quanto ao servidor da \acrshort{utfpr}, adotou-se inicialmente a suposição de que a configuração do \textit{Coolify} deveria ser feita apenas no campus, por receio de expor o painel de administração e por inexistência de acesso remoto à máquina. Essa premissa levou a deslocamentos frequentes à universidade sempre que era preciso ajustar serviços, variáveis de ambiente ou implantações. Somente nas fases finais do trabalho, com o banco de dados, a topologia de containers e as funcionalidades centrais já consolidados no repositório, concentraram-se no campus a publicação dos ambientes de \textit{staging} e de produção nos domínios staging.e-museu.gp.utfpr.edu.br e e-museu.gp.utfpr.edu.br, respectivamente, com \textit{MySQL}, \textit{Redis}, \textit{MinIO} e a aplicação Laravel provisionados pelo \textit{Coolify} (Seção~\ref{sec:materiais-coolify}).


\section{Refatoração da aplicação}\label{sec:dev-refatoracao}

A refatoração concentrou-se em atualizar a pilha tecnológica (PHP e Laravel, conforme o Capítulo~\ref{cap:materiais-e-metodos}), reduzir acoplamento nos controladores legados e reorganizar o código segundo boas práticas citadas no referencial (\acrshort{mvc}, \textit{Clean Code} e organização por domínio). Uma das mudanças estruturais mais relevantes foi a adoção de contextos delimitados inspirados no \acrshort{ddd} (Seção~\ref{sec:ref-ddd}).


\subsection{Organização por domínios (\acrshort{ddd})}\label{sec:dev-ddd}

Antes da reorganização, grande parte das classes de aplicação permanecia em pastas genéricas (\texttt{app/Http/Controllers}, \texttt{app/Services}, \texttt{app/Models}), o que dificultava localizar regras ligadas ao acervo, à taxonomia ou aos colaboradores. A partir da \textit{issue} de refatoração ``organize the project into domains'' e dos \textit{pull requests} associados (por exemplo, \#10 e \#12 no repositório do projeto), o código passou a espelhar a linguagem do museu: cada domínio concentra modelos Eloquent, serviços, ações, requisições de validação e, quando aplicável, controladores HTTP e \textit{seeders}/\textit{factories} de testes.

Adicionalmente, o vocabulário do sistema foi alinhado ao negócio: o antigo termo técnico \textit{proprietary} (proprietário da peça) foi renomeado para colaborador (\textit{collaborator}), refletindo que as contribuições vêm de participantes externos verificados por e-mail, e não de um único dono do item. Categorias de item e etiquetas também receberam nomes estáveis (\texttt{item\_category}, \texttt{tag\_category}) para evitar ambiguidade com ``seções'' ou ``tags'' genéricas.

A Tabela~\ref{tab:dominios-emuseu} resume os domínios adotados no repositório atual. Entidades de apoio usadas por mais de um contexto (\texttt{Language}, \texttt{Location}) permanecem em \texttt{app/Models} na raiz, com serviço dedicado para localização física das peças; a internacionalização de textos aparece em modelos de tradução por domínio e no enum \texttt{ContentLanguage}.

\newcommand{\camadas}[1]{%
    \par\vspace{0.35em}%
    \noindent\textit{Organização no código:}%
    \begin{itemize}[nosep,leftmargin=1.25em,topsep=2pt,partopsep=0pt]#1\end{itemize}%
}

\begin{table}[H]
    \centering
    \caption{Domínios do E-Museu e principais responsabilidades no código.}
    \label{tab:dominios-emuseu}
    \footnotesize
    \setlength{\tabcolsep}{6pt}
    \renewcommand{\arraystretch}{1.25}
    \begin{tabularx}{\textwidth}{@{}
        >{\raggedright\arraybackslash}p{0.14\textwidth}
        >{\raggedright\arraybackslash}X
    @{}}
        \toprule
        Domínio & Responsabilidade e organização em \texttt{app/} \\
        \midrule
        Catalog &
        Núcleo do acervo: itens, traduções, imagens, componentes, vínculos com etiquetas, categorias de item, \textit{extras} e fluxos públicos de contribuição e de consulta.
        \camadas{%
            \item \texttt{Models/Catalog}, \texttt{Services/Catalog} e \texttt{Actions/Catalog};
            \item \texttt{Http/Controllers/Catalog} (catálogo público);
            \item \texttt{Http/Controllers/Admin/Catalog} (painel administrativo).
        } \\
        Taxonomy &
        Classificação transversal: etiquetas (\textit{tags}) e categorias de etiqueta, com textos por idioma.
        \camadas{%
            \item \texttt{Models/Taxonomy} e \texttt{Services/Taxonomy};
            \item \texttt{Http/Controllers/Admin/Taxonomy};
            \item \texttt{Http/Requests/Taxonomy}.
        } \\
        Collaborator &
        Pessoas que contribuem com itens ou \textit{extras}: cadastro, papéis, bloqueio, verificação de e-mail e elegibilidade para o catálogo público.
        \camadas{%
            \item \texttt{Models/Collaborator} e \texttt{Services/Collaborator};
            \item \texttt{Http/Controllers/Catalog/CollaboratorController} (contribuição pública);
            \item \texttt{Http/Controllers/Admin/Collaborator};
            \item \texttt{Enums/Collaborator} e mensagens em \texttt{Mail/}.
        } \\
        Identity &
        Identidade administrativa: contas de curadoria, autenticação de \textit{login} e travas de edição concorrente (\textit{locks}) em telas sensíveis.
        \camadas{%
            \item \texttt{Models/Identity} e \texttt{Services/Identity};
            \item \texttt{Http/Controllers/Admin/Identity};
            \item \texttt{Http/Middleware/Auth};
            \item \texttt{Console/Commands/Identity}.
        } \\
        Admin (IA) &
        Tradução assistida de conteúdos multilíngues no painel (itens, categorias, etiquetas), com provedores externos de \textit{chat completion}.
        \camadas{%
            \item \texttt{Actions/Admin/Ai};
            \item \texttt{Support/Admin/Ai} e \texttt{Client/Ai};
            \item \texttt{Http/Controllers/Admin/Ai}.
        } \\
        \bottomrule
    \end{tabularx}
    \caption*{\textbf{Fonte:} Autoria própria, com base na estrutura do repositório \texttt{e-museu} (versão refatorada). \textbf{Nota:} \texttt{Language} e \texttt{Location} permanecem em \texttt{app/Models/} na raiz; \texttt{StorageProxyController} e \texttt{Support/} tratam infraestrutura transversal.}
\end{table}

Dentro de cada domínio, a refatoração manteve uma divisão de papéis coerente com o \textit{framework}: controladores finos (principalmente orquestração HTTP), \texttt{Actions} para casos de uso compostos (por exemplo, \texttt{StoreItemContributionAction} e \texttt{RegenerateItemQrCodeAction} no catálogo), \texttt{Services} para regras de aplicação e montagem de dados de visão, e modelos Eloquent para persistência. Validações de entrada ficam em \texttt{Http/Requests} espelhando o mesmo agrupamento (\texttt{Catalog}, \texttt{Taxonomy}, \texttt{Collaborator}, \texttt{Admin/...}).

No domínio Catalog, os modelos centrais são: \texttt{Item} e \texttt{ItemTranslation} (peça e textos por idioma), \texttt{ItemImage} (fotos e QR), \texttt{ItemComponent} (subpartes da peça), \texttt{ItemTag} (associação item--etiqueta), \texttt{ItemCategory} e \texttt{ItemCategoryTranslation}, além de \texttt{Extra} e \texttt{ExtraTranslation} (contribuições complementares da comunidade). No domínio Taxonomy: \texttt{Tag}, \texttt{TagTranslation}, \texttt{TagCategory} e \texttt{TagCategoryTranslation}. Em Collaborator: o modelo \texttt{Collaborator} concentra nome, e-mail, papel e estado de verificação. Em Identity: \texttt{Admin} e \texttt{Lock}.

A camada de dados de testes e carga inicial segue a mesma lógica: \textit{factories} e \textit{seeders} em subpastas \texttt{Catalog}, \texttt{Taxonomy}, \texttt{Collaborator} e \texttt{Identity}, com mapeamento explícito em \texttt{config/factory\_model\_map.php} para o Laravel resolver corretamente modelo e \textit{factory} após a mudança de \textit{namespace}.

% TODO: atualização Laravel/PHP (detalhes de migração de versão); exemplos pontuais de refatoração de controladores (figuras ou trechos em apêndice).


\section{Testes automatizados e qualidade de código}\label{sec:dev-testes}

% TODO: linters, testes de fluxo/integração/usabilidade conforme objetivos;
% ferramentas adotadas e cobertura alcançada (valores a confirmar pelo autor).


\section{Funcionalidades implementadas}\label{sec:dev-funcionalidades}

% TODO, alinhado aos objetivos específicos:
% - upload múltiplo de fotos e link de vídeo;
% - internacionalização;
% - reCAPTCHA e segurança;
% - geração de etiquetas para peças físicas;
% - demais entregas priorizadas no MoSCoW.


\section{Implantação, validação e entrega institucional}\label{sec:dev-deploy}

\subsection{Automação de \textit{deploy} e acesso ao \textit{Coolify}}

A automação de \textit{deploy} na \acrshort{utfpr} exigiu iterações sucessivas. Na primeira implantação presencial de \textit{staging} e produção, os \textit{webhooks} nativos do \textit{Coolify} ligados ao repositório não passaram a disparar atualizações de forma confiável após \textit{commits} no \textit{GitHub}. Para contornar o problema, foram experimentadas duas abordagens alternativas antes da solução definitiva.

Na \textbf{primeira abordagem}, implementou-se um script agendado no próprio \textit{Coolify} (\texttt{coolify-auto-deploy.sh}), executado por tarefa periódica no container da aplicação: o script consultava o repositório remoto, comparava o \textit{hash} do último \textit{commit} com um arquivo de estado e, havendo mudança, acionava a API de \textit{deploy} do \textit{Coolify} com token de autorização. A solução funcionou, porém consumia processamento de forma contínua e acrescentava complexidade operacional.

Na \textbf{segunda abordagem}, criou-se um \textit{endpoint} na aplicação Laravel para receber gatilhos do \textit{GitHub Actions} a cada alteração no repositório, com pipeline dedicada de implantação. O fluxo passou a operar parcialmente, mas persistiram falhas na autenticação entre o \textit{GitHub Actions} e o ambiente institucional (incluindo incompatibilidade de formato de token que respondia em testes manuais, mas não na \textit{action}), o que mantinha a dependência de idas ao campus para corrigir configurações.

Próximo ao encerramento do projeto, o coorientador Dr. Diego Marczal orientou a \textbf{exposição do painel do \textit{Coolify} em subdomínio dedicado}, em \url{https://coolify.e-museu.gp.utfpr.edu.br/}, em lugar de tratar o painel apenas como serviço acessível somente na rede interna do servidor. A decisão equilibrou praticidade e segurança: o painel passou a ser administrado remotamente quando necessário (configuração de projetos, variáveis de ambiente e disparo de \textit{deploys}), com autenticação em dois fatores habilitada no \textit{Coolify}, sem abandonar o controle institucional sobre a máquina. Com o subdomínio configurado, removeram-se do repositório o \textit{endpoint} de \textit{deploy} na aplicação e a \textit{action} associada, adotando-se de forma estável os \textit{webhooks} do \textit{GitHub} para o \textit{Coolify}, conforme sugerido na orientação. Esse desfecho reduziu a necessidade de deslocamentos repetidos à \acrshort{utfpr} apenas para operações rotineiras de infraestrutura, concentrando as visitas presenciais em momentos de carga de dados, validação ou ajustes que ainda exigem acesso local ao servidor.

\subsection{Validação em \textit{staging} e entrega}

Após cada integração relevante na ramificação \texttt{develop}, o sistema era validado em \textit{staging} antes da promoção à produção (\texttt{main}). Quando surgiam falhas, o ciclo retornava ao desenvolvimento local, à correção via \textit{pull requests} e aos testes automatizados descritos na Seção~\ref{sec:dev-testes}, até nova implantação. Ao final, consolidou-se a entrega operacional nos domínios institucionais, com documentação e artefatos de continuidade preparados para apoiar manutenção futura do ambiente, sem expor credenciais neste documento.


\section{Síntese dos resultados alcançados}\label{sec:dev-sintese-resultados}

Ao término das etapas descritas neste capítulo, consolidam-se os principais resultados do trabalho em relação ao objetivo geral (Seção~\ref{subsec:objetivoGeral}) e aos objetivos específicos (Seção~\ref{subsec:objetivoEspecificos}). A Tabela~\ref{tab:objetivos-resultados} resume, de forma sintética, o atendimento a cada meta e indica onde a evidência correspondente é detalhada no texto. Itens marcados como parciais ou não atendidos são discutidos nas limitações e nos trabalhos futuros (Seções~\ref{sec:conclusao-limitacoes} e~\ref{sec:conclusao-trabalhos-futuros}).

\begin{table}[H]
    \centering
    \caption{Síntese dos objetivos específicos e dos resultados alcançados.}
    \label{tab:objetivos-resultados}
    \begin{tabularx}{\textwidth}{@{}p{0.36\textwidth}Xp{0.14\textwidth}@{}}
        \toprule
        \textbf{Objetivo específico} & \textbf{Principal evidência no trabalho} & \textbf{Situação} \\
        \midrule
        Versionamento e padrões no GitHub &
        Seções~\ref{sec:dev-gitflow} e~\ref{sec:dev-requisitos} &
        % TODO: Atendido / Parcial / Não atendido
        a confirmar \\
        Ambientes de desenvolvimento, teste e produção &
        Seções~\ref{sec:dev-infra} e~\ref{sec:dev-deploy} &
        a confirmar \\
        Refatoração do código existente &
        Seção~\ref{sec:dev-refatoracao} &
        a confirmar \\
        Testes automatizados &
        Seção~\ref{sec:dev-testes} &
        a confirmar \\
        Novas funcionalidades do E-Museu &
        Seção~\ref{sec:dev-funcionalidades} &
        a confirmar \\
        Padrões de projeto e boas práticas &
        Seções~\ref{sec:dev-refatoracao} e~\ref{sec:dev-funcionalidades}; Capítulo~\ref{cap-referencial-teorico} &
        a confirmar \\
        \bottomrule
    \end{tabularx}
    \caption*{\textbf{Fonte:} Autoria própria. \textbf{Nota:} Preencher a coluna ``Situação'' após fechar o capítulo (por exemplo: atendido, parcial ou não abordado neste TCC).}
\end{table}

% TODO: parágrafo fechando: sistema em produção (e-museu.gp.utfpr.edu.br), comparação
% sucinta com a versão de \citeonline{gonzaga2024} e principais ganhos mensuráveis
% (ex.: ambientes separados, documentação de fluxo, quantidade de testes — confirmar números).

