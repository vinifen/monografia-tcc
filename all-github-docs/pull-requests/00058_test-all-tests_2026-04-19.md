---
repository: e-museu-utfpr-gp/e-museu
github_pull_request_number: 58
github_pull_request_id: 3552341305
html_url: https://github.com/e-museu-utfpr-gp/e-museu/pull/58
state: closed
draft: False
author: vinifen
created_at: 2026-04-19T16:18:57Z
updated_at: 2026-04-24T22:07:19Z
merged_at: 2026-04-19T20:29:48Z
closed_at: 2026-04-19T20:29:48Z
head_ref: @vinifen/test/57/all-tests
base_ref: develop
title: "🧪 test: all tests"
---

# 🧪 test: all tests

# 🧪 All tests
<!-- Put the emoji related to the type and use Title Case or Sentence case for the issue title -->

### Related Issue: #57
<!-- Use: "Related Issue: N/A" if no issue -->

## 🔄 Change Overview

### ✨ Summary of changes:
<!-- Briefly describe what was done and why, preferably in bullet points -->
- **⚙️ chore: utfpr email support, fixes, first tests:**
```md
## Mail & configuration

- **SMTP** is the primary outbound path; **`config/mail.php`** defines UTFPR-oriented defaults (host, port, mailbox user, `From` address) and default mailer **`smtp`**.
- **Resend** was removed end-to-end: **`composer.json` / `composer.lock`**, **`config/services.php`**, mailer config, and any app-level references.
- **`App\Support\Mail\OutgoingMailIsConfigured`** unchanged in behaviour for SMTP (host must be non-empty); unit tests updated for SMTP/failover (no Resend).
- **`phpunit.xml`** still forces **`MAIL_MAILER=array`** so tests do not open real SMTP connections.

## Environment & deploy documentation

- **`.env.example`**: mail variables listed with inline notes; defaults aligned with `config/mail.php`; **`REDIS_URL=`** restored where it was missing; header text updated to point at `MAIL_*`; anti-bot and other vars grouped for clarity.
- **`deploy-docs/coolify-production.env.example`** and **`deploy-docs/coolify-staging.env.example`**: minimal paste-friendly templates (e.g. **`MAIL_PASSWORD`**, plus stack placeholders); staging includes **`STAGING_HTTP_*`** for Basic Auth.
- **`deploy-docs/REVIEW_ENV_MAIL_CHANGES.md`**: audit of risks (empty `MAIL_*` overrides, SMTP “ready” without password check, antibot vs PHPUnit, etc.) and optional follow-ups.

## README

- **Outgoing mail** section updated for SMTP defaults, minimal Coolify env, and local **`MAIL_MAILER=log`** guidance.

## Frontend / catalog

- **`resources/js/pages/admin/collaborators/check-contact.js`**: **HTTP 422** (invalid/incomplete email while typing) no longer shows the “network or server” error; avoids false “offline” feedback.

## PHP quality

- **`tests/Unit/Http/Controllers/Admin/Auth/AdminLoginControllerTest.php`**: PSR-12 fix — **`new AdminLoginController()`** (parentheses required).
```
- **🧪 test: all-tests:**

```md
### Test infrastructure

- **`AbstractMysqlRefreshDatabaseTestCase`** and **`RequiresMysqlDriverConnection`**: shared MySQL-only Feature setup and consistent skip when the driver is not MySQL.
- **`MinimalContributionCoverJpeg`**: single tiny JPEG fixture for uploads without GD.
- **`ServiceMysqlTestCase`** / **`MysqlMiddlewareTestCase`**: base cases for Unit services and Feature middleware tests that need a real DB.
- **`phpunit.xml`** and **`README.md`**: testing DB and MySQL-group documentation updated.

### Feature tests (high level)

- Broad **admin** coverage: catalog items, categories, components, tags, extras, collaborators, identity (admin list, release lock), taxonomy (tag / tag category).
- **Catalog** public flows: items, extras, tags, collaborator e-mail verification, contribution dates, optional JSON locale, identification redirect, location filter, **translation resolution** (renamed to `TranslationResolutionFeatureTest`).
- **Mail error JSON / routes**: `CatalogVerifyMailErrorRouteTest` (renamed from older name in the diff).
- **Middleware**: authenticate, redirect-if-authenticated, locale-from-session, staging basic auth, verify antibot challenge.
- **AntiBot**, **Home**, **Storage proxy**, **Create admin** console command, **Example** Feature redirect.

### Unit tests

- **Actions**: `StoreItemContributionActionTest`.
- **HTTP**: `ItemContributionValidatorTest`; **Admin login** Unit renamed to **`AdminLoginControllerUsernameFieldTest`** (no name clash with Feature `AdminLoginControllerTest`).
- **Services**: catalog (items, categories, components, tags, images, QR, extras, contribution mail/completion, content locale), collaborator verification/collaborator service, identity (admin, locks), locations, taxonomy.
- **Support / pure logic**: admin index config/query/table/rules, catalog resolvers and verify-mail JSON, content translation/SQL/payload, mail verification code, optional content locale, antibot verifier, public contribution outcome.

### Application code (minor, outside tests)

- **`CreateAdmin`**: command prompts and messages switched to **English** (aligns with project test / copy convention).
- **`TagService`**: `getValidatedNamesForAutocomplete` adds **`->select('tags.id')`** before `selectRaw` so the query shape is explicit (supports autocomplete / tests).

## Hygiene / consistency (included in the same effort)

- Removed placeholder **`tests/Unit/ExampleTest.php`**.
- Prefer **`app(Service::class)`** over `new Service()` in Unit service tests where no manual wiring is required.
- **`REVISAO-TESTES-SEGUNDA-PASSAGEM.md`**: Portuguese review doc updated with “state after fixes” (session/config teardowns, renames, etc.) — see that file for the detailed checklist.
``` 

### 🏷️ Type of change:
<!-- Select one or two that apply -->
- **TEST, CHORE**

### ⏳ Time spent:
- **6 HOURS** (approximate)

### 📝 Additional notes:
<!-- Any extra context, screenshots, or information for the reviewer -->
- N/A

## ✅ PR Checklist
- [x] No sensitive information (passwords, API keys, secrets) exposed
- [x] All tests run and pass successfully (unit, e2e, lint, etc.)
- [x] Code compiles and runs without errors
- [x] Tests and documentation updated or added if applicable
- [x] Removed unnecessary comments, debug statements and dev artifacts
- [x] Related issues, labels, and PR links established; PR title OK
